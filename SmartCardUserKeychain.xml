<?xml version="1.0" encoding="UTF-8"?>
<extensionAttribute>
	<displayName>SmartCard - User's Keychain</displayName>
	<displayInCategory>Operating System</displayInCategory>
	<dataType>string</dataType>
	<description>Returns the SmartCard(s) currently available in the user's keychain via system_profiler SPSmartCardsDataType.</description>
	<scriptContentsMac>#!/bin/zsh

# Updated by SecurityShrimp 2026
# SmartCard User Keychain Extension Attribute
# Targets macOS 12 through 26 (Tahoe), Intel and Apple Silicon

cards=$(/usr/sbin/system_profiler SPSmartCardsDataType 2&gt;/dev/null \
	| /usr/bin/awk '
		/Available SmartCards \(keychain\):/ { found=1; next }
		found &amp;&amp; /^[[:space:]]*[A-Za-z]/ { gsub(/^[[:space:]]+/, ""); print; exit }
	')

echo "&lt;result&gt;${cards:-Not Available}&lt;/result&gt;"
</scriptContentsMac>
</extensionAttribute>
