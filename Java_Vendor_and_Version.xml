<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE extensionAttribute PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<extensionAttribute>
	<displayName>Java Vendor and Version</displayName>
	<displayInCategory/>
	<dataType>string</dataType>
	<description>Reports the installed Java runtime vendor and version. Apple stopped shipping Java entirely after 10.7, so on modern macOS this reports the third-party JDK (Oracle, Azul, Temurin, etc.) detected via /usr/libexec/java_home.</description>
	<scriptContentsMac>#!/bin/zsh

# Updated by SecurityShrimp 2026
# Java Vendor and Version Extension Attribute
# Targets macOS 12 through 26 (Tahoe), Intel and Apple Silicon

if ! /usr/libexec/java_home -F &amp;&gt;/dev/null; then
    echo "&lt;result&gt;No Java Available&lt;/result&gt;"
    exit 0
fi

out=$(/usr/bin/java -version 2&gt;&amp;1)
version=$(echo "$out" | /usr/bin/awk -F'"' '/version/ { print $2; exit }')

if echo "$out" | /usr/bin/grep -qi "openjdk"; then
    vendor="OpenJDK"
elif echo "$out" | /usr/bin/grep -qi "java(tm)"; then
    vendor="Oracle"
else
    vendor="Unknown"
fi

echo "&lt;result&gt;${vendor} ${version:-Unknown}&lt;/result&gt;"
</scriptContentsMac>
</extensionAttribute>
