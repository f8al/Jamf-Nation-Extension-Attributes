<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE extensionAttribute PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<extensionAttribute>
	<displayName>Quest QAS Version Info</displayName>
	<displayInCategory/>
	<dataType>string</dataType>
	<description>Returns the installed Quest/One Identity Authentication Services version via vastool, or "Not Installed". QAS was rebranded "Safeguard Authentication Services".</description>
	<scriptContentsMac>#!/bin/zsh

# Updated by SecurityShrimp 2026
# Quest QAS Version Extension Attribute
# Targets macOS 12 through 26 (Tahoe), Intel and Apple Silicon

if ! /usr/bin/which vastool &amp;&gt;/dev/null &amp;&amp; [[ ! -x /opt/quest/bin/vastool ]]; then
	echo "&lt;result&gt;Not Installed&lt;/result&gt;"
	exit 0
fi

vastool="${commands[vastool]:-/opt/quest/bin/vastool}"
version=$("$vastool" -v 2&gt;/dev/null | /usr/bin/awk '/QAS Version|Safeguard Authentication Services/ { print $NF; exit }')

echo "&lt;result&gt;${version:-Unknown}&lt;/result&gt;"
</scriptContentsMac>
</extensionAttribute>
