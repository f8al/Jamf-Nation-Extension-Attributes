<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE extensionAttribute PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<extensionAttribute>
	<displayName>Java Vendor</displayName>
	<displayInCategory/>
	<dataType>string</dataType>
	<description>Detects which JDK vendor is installed (Oracle, OpenJDK/Temurin/Zulu, etc.) or "No Java Available".</description>
	<scriptContentsMac>#!/bin/zsh

# Updated by SecurityShrimp 2026
# Java Vendor Extension Attribute
# Targets macOS 12 through 26 (Tahoe), Intel and Apple Silicon

if ! /usr/libexec/java_home -F &amp;&gt;/dev/null; then
    echo "&lt;result&gt;No Java Available&lt;/result&gt;"
    exit 0
fi

out=$(/usr/bin/java -version 2&gt;&amp;1)

if echo "$out" | /usr/bin/grep -qi "openjdk"; then
    echo "&lt;result&gt;OpenJDK&lt;/result&gt;"
elif echo "$out" | /usr/bin/grep -qi "java(tm)"; then
    echo "&lt;result&gt;Oracle&lt;/result&gt;"
else
    echo "&lt;result&gt;Unknown&lt;/result&gt;"
fi
</scriptContentsMac>
</extensionAttribute>
