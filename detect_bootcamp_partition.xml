<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE extensionAttribute PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<extensionAttribute>
	<displayName>Detect Bootcamp Partition</displayName>
	<displayInCategory/>
	<dataType>string</dataType>
	<description>Reports "Yes" if a Microsoft Basic Data partition is present, "No" otherwise. Returns "N/A" on Apple Silicon (Boot Camp is Intel-only).</description>
	<scriptContentsMac>#!/bin/zsh

# Updated by SecurityShrimp 2026
# Detect Boot Camp Partition Extension Attribute
# Targets macOS 12 through 26 (Tahoe), Intel only
# Boot Camp doesn't exist on Apple Silicon. On Intel, a Boot Camp install
# creates a "Microsoft Basic Data" partition that diskutil enumerates.

if [[ "$(/usr/bin/uname -m)" == "arm64" ]]; then
    echo "&lt;result&gt;N/A&lt;/result&gt;"
    exit 0
fi

if /usr/sbin/diskutil list 2&gt;/dev/null | /usr/bin/grep -q "Microsoft Basic Data"; then
    echo "&lt;result&gt;Yes&lt;/result&gt;"
else
    echo "&lt;result&gt;No&lt;/result&gt;"
fi
</scriptContentsMac>
</extensionAttribute>
